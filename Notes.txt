Install packages "python -m pip install wheel, build, setuptools"
Install the package "python -m pip install pipenv"
Create environment (Pipfile and Pipfile.lock) "pipenv install"
Create setup.py in root of the project
To create a wheel "python -m build --wheel"

